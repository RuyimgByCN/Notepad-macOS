.DS_Store
*.zip

.build/
.swiftpm/
dist/

# Upstream reference repos — fetched fresh locally for development
# (not version-controlled; treated as local clones for reference)
upstream/notepad-plus-plus/
upstream/notepad--/
upstream/NotepadNext/

# Claude Code local agent worktrees — never version-controlled
.claude/worktrees/

# Python bytecode caches (from patch scripts)
__pycache__/

